<h2>All Users</h2>
<% users.forEach(user => { %>
  <div>
      <%= user.username %>
      <%= user.password %>
  </div>
  <a href="/users/<%= user.id %>">View</a>
  <%- include('../partials/deleteForm', { url: `/admin/${user.id}` }) %>
<% }) %>